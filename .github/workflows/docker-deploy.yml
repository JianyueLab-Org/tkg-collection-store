name: Docker Deploy

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. 转换名称并存入环境变量（为了后续部署使用）
      - name: lowercase repo name
        run: |
          echo "IMAGE_NAME_LOWER=${GITHUB_REPOSITORY@L}" >> "${GITHUB_ENV}"

      # 2. 提取 Docker 标签元数据（非常重要，否则 build-push 没标签可用）
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}
          # --- 核心修改：明确定义标签策略 ---
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=ref,event=branch
          # --------------------------------

      # 3. 设置 Buildx（支持缓存导出的关键）
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4. 登录 GHCR
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 5. 构建并推送（仅推送到 GHCR）
      - name: Build and push to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ env.IMAGE_NAME_LOWER }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

# 因为GHCR访问太慢，我决定自行实现部署，这里先注释掉，后续如果需要可以再启用
#  deploy:
#    needs: build-and-push
#    runs-on: ubuntu-latest
#    steps:
#      - name: lowercase repo name
#        run: |
#          echo "IMAGE_NAME_LOWER=${GITHUB_REPOSITORY@L}" >> "${GITHUB_ENV}"
#      - name: Deploy via SSH
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USER }}
#          key: ${{ secrets.SSH_KEY }}
#          # 建议增加超时，防止拉取镜像过慢导致中断
#          command_timeout: 20m
#          script: |
#            # 使用 GHCR 镜像进行部署
#            GHCR_IMAGE="ghcr.io/${{ env.IMAGE_NAME_LOWER }}:latest"
#
#            docker pull $GHCR_IMAGE
#            docker stop svelte-app || true
#            docker rm svelte-app || true
#
#            # 在宿主机创建一个目录用来放数据库，防止权限问题
#            mkdir -p /home/ubuntu/app_data && chmod 777 /home/ubuntu/app_data
#
#            # 启动容器时进行挂载
#            docker run -d \
#              --name svelte-app \
#              -p 8080:3000 \
#              -v /home/ubuntu/app_data:/app/data \
#              -e DATABASE_PATH=/app/data/production.sqlite \
#              $GHCR_IMAGE
#
#            # 清理虚悬镜像，节省宿主机空间
#            docker image prune -f